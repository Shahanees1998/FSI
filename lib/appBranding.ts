/** Application display name (UI, metadata, emails, default agency label). */
export const APP_NAME = "JS Investment";

export const APP_PORTAL_NAME = `${APP_NAME} Portal`;

export const APP_DEFAULT_AGENCY_NAME = APP_NAME;

/** Shown when no `SystemSettings` row exists yet; override in Admin → Settings. */
export const APP_DEFAULT_SUPPORT_EMAIL = "support@jsinvestment.com";
